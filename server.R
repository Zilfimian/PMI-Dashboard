library(DBI)
library(RMySQL)


server <- function(input, output) {
  
  #---------------------------------------------------------------------------------------------------------
  #--------------------------------------------------------------- Tab 1 -----------------------------------
  #---------------------------------------------------------------------------------------------------------
  
  
  output$UI1 <- renderUI({
    
    fluidRow(class = "text-center & center",
             box(width = 12, solidHeader = TRUE,
                 tabBox(
                   width = 12,
                   side = "right",
                   selected = "Sales Analysis",
                   tabPanel(
                     title = "Sales Analysis", 
                     valueBox(value = "", subtitle = str_to_title("Sales Analysis"), color = "purple", width = 12),
                     box(width = 12, solidHeader = TRUE),
                     box(width = 12, solidHeader = TRUE)
                   ),
                   tabPanel(
                     title = "Customer demographics", 
                     h6("Interpolation count period.")
                     #,formattableOutput("summary_table_2")
                   ),
                   tabPanel(
                     title = "Product Analysis", 
                     h6("riod.")
                     #,formattableOutput("summary_table_2")
                   )
                 ) # tabBox
             )
    )
  
  })# UI1
  
  
  
  
  
  # Example of rendering dynamic table based on user selections
  dynamic_table <- function(data, vars){
    
    vars = paste0(vars, collapse = ", ")
    data_ <- dbGetQuery(con, paste("SELECT", vars, "FROM", data))
    return(data_)
  }

  
  output$sales_amount <- reactive({
   
    })
    
  
  
  output$Sales_Plot <- renderPlotly({


  })
  
  output$summary_table <- renderDataTable({
    
    
    
  })
  
  #---------------------------------------------------------------------------------------------------------
  #--------------------------------------------------------------- Tab 2 -----------------------------------
  #---------------------------------------------------------------------------------------------------------
  
  
  
  
  #---------------------------------------------------------------------------------------------------------
  #--------------------------------------------------------------- Tab 3 -----------------------------------
  #---------------------------------------------------------------------------------------------------------
  
  
  
  
  
  #---------------------------------------------------------------------------------------------------------
  #--------------------------------------------------------------- Tab 4 -----------------------------------
  #---------------------------------------------------------------------------------------------------------
  
  
  
  
  #---------------------------------------------------------------------------------------------------------
  #--------------------------------------------------------------- Tab 5 -----------------------------------
  #---------------------------------------------------------------------------------------------------------
  
  
  
}# server
  